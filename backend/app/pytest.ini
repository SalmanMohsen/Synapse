[pytest]
asyncio_mode = auto
testpaths = auth/tests workspace/tests project/tests channel/tests ticket/tests agent_run/tests inbox/tests git_providers/tests tests_integration
markers =
    integration: requires real Postgres/Redis via testcontainers (Docker)
pythonpath = ..